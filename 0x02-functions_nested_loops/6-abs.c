#include "main.h"

/**
 ** abs - prints abs value of an int
 ** @n: the number
 ** Return: return the abs of the number
 **/
int abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -);
	}
}

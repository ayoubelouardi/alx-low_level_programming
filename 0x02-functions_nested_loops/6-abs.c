#include "main.h"

/**
 ** _abs - prints abs value of an int
 ** @n: the number
 ** Return: return the abs of the number
 **/
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}

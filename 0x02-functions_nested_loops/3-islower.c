#include "main.h"



/**
 * _islower - Entry point
 * @c: adsf
 * Description: Longer description of the function)?
 * Return: 0 if success
 */
int _islower(int c)
{
	if ('a' < c && c < 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

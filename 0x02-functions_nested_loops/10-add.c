#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * Description: Longer description of the function)?
 * Return: 0 if success
 */

int main(void)
{
	int n;

	n = add(89, 9);
	printf("%d\n", n);
	return (0);
}

/**
 * add - Entry point
 * @a: sfd
 * @b: afd
 * Description: Longer description of the function)?
 * Return: 0 if success
 */
int add(int a, int b)
{
	return (a + b);
}
